name: Deploy dev ennotrace fe
run-name: ${{ github.actor }} is deploying dev branch 🚀
on:
  push:
    branches:
      - "develop"
jobs:
  Deploy:
    runs-on: [self-hosted, runner_ennovie_fe_develop]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Use Node.js 16.13
        uses: actions/setup-node@v2
        with:
          node-version: 16.13
      - run: |
          echo "${{ vars.ENNOVIE_DEV }}" >> .env.development
          yarn
          pm2 reload pm2.json
